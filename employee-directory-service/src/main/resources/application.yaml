spring:
  application:
    name: employee-directory-service

  datasource:
    url: jdbc:postgresql://localhost:5432/employeedirectorydb
    username: postgres
    password: siraj123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

grpc:
  server:
    port: 9090

logging:
  level:
    root: INFO
    com.sirajchaudhary.employeedirectory: DEBUG